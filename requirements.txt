# --- Core numerics (Python 3.13 + NumPy 2) ---
numpy>=2.0,<3
pandas>=2.3,<2.5
scipy>=1.12,<2.0
scikit-learn>=1.5,<1.7
statsmodels>=0.14.1,<0.15
pyarrow>=16,<19

# --- ML toolkits ---
torch>=2.5,<2.7           # CPU build is fine for the E32as_v5
xgboost>=2.0,<2.3

# --- Viz / UX ---
matplotlib>=3.9,<4
plotly>=5.22,<6
tqdm>=4.66,<5

# --- IO / Infra ---
requests>=2.31,<3
requests_cache>=1.2,<2
python-dotenv>=1.0,<2
pyyaml>=6.0,<7
paramiko>=3.4,<4

# --- Data sources (install only if you actually use them) ---
wrds>=3.1,<4
psycopg2-binary>=2.9,<3
openassetpricing

# --- Dev / tooling (optional) ---
ipykernel>=6.29,<7
ruff>=0.5,<1
